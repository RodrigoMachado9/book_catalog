# get parameters in query string  | example.

@app.route('/new')
def query_string():
    # fixme, get query string..
    query = request.args.get("greeting")
    # fixme, payload data. .
    payload = request.json
    print(payload)
    return "<h1>the greeting is : {0}</h1>".format(query)


# paramenters explicit

@app.route('/demo/string/<string:name>')
def route_by_string(name):
    try:
        return "hello {0}".format(name)
    except Exception:
        return None
